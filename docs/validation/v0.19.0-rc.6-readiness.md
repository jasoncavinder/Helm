# v0.19.0-rc.6 Readiness Validation

This document records the focused acceptance pass for the changes merged after
`v0.19.0-rc.5`. Candidate readiness remains distinct from the protected
integration and publication evidence recorded below.

## Candidate Baseline

- [x] Validation used merged `dev` revision `d8a029a5e3bdc65c56d71056be2f192b3d7cbbc1` after PR `#451`.
- [x] A fresh arm64 Debug app was built from that revision and used the `Helm-Development` database namespace.
- [x] The app executable was arm64, the embedded XPC service was present, and the development-signed bundle passed deep strict code-signature verification.
- [x] Fresh discovery populated the development database with real local manager and package state rather than fixture-only data.

## Automated Gates

- [x] The full repository quality gate passed on 2026-08-14.
- [x] Rust workspace tests, formatting, and Clippy passed.
- [x] Locale integrity, overflow, mirror parity, and channel-policy checks passed.
- [x] The arm64 macOS suite passed all 144 tests while retaining the macOS 13 deployment target.
- [x] Release-state, toolchain, provenance, recovery, and stable/RC appcast-channel contracts passed.
- [x] Non-mutating release preflight and prepare checks passed. The only warning was that GitHub token scopes could not be inferred from response headers.
- [x] The Sparkle/appcast checklist passed, including appcast policy, recovery, release-line copy, and rehearsal dry-run contracts.

Version-preparation evidence: on 2026-08-14, the full quality gate passed again
after the Rust workspace moved to `0.19.0-rc.6`, including all 144 arm64 macOS
tests and release contracts. The explicit non-mutating RC6 Sparkle/appcast
checklist and release rehearsal then passed preflight, prepare, and verify.

## Settings Window

- [x] Settings uses one leading sidebar toggle and a centered plain `Helm Settings` title without the duplicate control or separator regressions.
- [x] The Wayfinder background extends through the title bar without changing the approved toolbar layout.
- [x] Settings remains visible but inactive above another app's full-screen Space whether or not Dashboard is also open.

## Third-Party Sparkle Handoff

Arc provided the concrete installed candidate for this pass: Helm discovered
`/Applications/Arc.app` at `1.159.0` with `1.160.0 (85122)` available.

- [x] Plan, Library, popover, and inspector surfaces present `Open App to Update` rather than claiming a direct Helm update.
- [x] The action opens the correct vendor app.
- [x] Helm does not directly start the app's Sparkle download or reproduce the installer-handoff error seen in RC5.
- [x] The vendor-owned Arc update completes normally, and a subsequent Helm refresh removes the resolved outdated row.

## Deferred Work

- True direct updating for eligible third-party Sparkle apps remains planned for `0.20.x` behind a dedicated signed/notarized updater boundary and verified completion contract.
- The unified popover redesign remains planned for `0.20.x`; it is not an RC6 requirement.
- The development-gated Environment Brief accessibility defect remains tracked separately by Issue `#388` and does not change the production first-run route.
- Native privileged-helper activation remains gated on its separate installed-candidate and security sequence; registration alone does not replace the existing askpass path.

## Outcome

- [x] The post-RC5 RC6 candidate scope is accepted on `dev`.
- [x] No release-blocking regression was found in the automated or owner-assisted pass.
- [x] RC6 version preparation may proceed through its own reviewed PR.
- [x] The Rust workspace and local packages identify as `0.19.0-rc.6` while published stable/RC metadata remains on `v0.18.2`/`v0.19.0-rc.5`.
- [x] After protected integration, the mandatory clean-`main` rehearsal, preflight, canary, and publication-authorization gates passed before creating tag `v0.19.0-rc.6`.

## Publication Closeout

- [x] Tag `v0.19.0-rc.6` identifies final candidate revision `968d0e400acc1a02a7e0a9a3d1a55917325189e8`.
- [x] The GitHub release is a published prerelease and does not replace stable `v0.18.2` as `releases/latest`.
- [x] Signed/notarized GUI workflow `31861811608` and direct CLI workflow `31861811594` completed successfully.
- [x] Publication PRs `#458` and `#459` merged RC CLI metadata plus the beta appcast and release notes into `main`.
- [x] Release verification and appcast/CLI drift guards passed on converged metadata revision `9214c3ea430ea7d3cf8a422d2f7399e5df026dd5`.
- [ ] Installed RC5-to-RC6 replacement, Settings, vendor-update, and preference-persistence checks remain owner validation after publication.
