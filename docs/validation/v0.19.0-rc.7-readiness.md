# v0.19.0-rc.7 Readiness Validation

This document records focused acceptance evidence for changes merged after
`v0.19.0-rc.6`. It does not claim that RC7 version preparation, protected
integration, or publication has completed.

## Candidate Baseline

- [x] Validation used merged `dev` revision `f276e2528312c9fcc1431edcb420a08538dc0168` after PRs `#467` and `#468`.
- [x] A fresh arm64 Debug app was built from that revision using the `Helm-Development` database namespace.
- [x] The app included its embedded XPC service and passed deep strict code-signature verification.
- [x] Real local manager, Library, and Plan data populated before connectivity testing.
- [x] Final automated validation used merged `dev` revision `16127dfff2050a2ba2d8b0b3502e110ab4460080` after PR `#470`.

## Connectivity

- [x] Transitioning from online to offline presented the localized Wayfinder offline state while preserving cached manager, Library, and Plan data.
- [x] Manual refresh while offline reconciled local inventory without producing failed-task fan-out.
- [x] Network-dependent search, install, upgrade, Plan execution, and Helm update work deferred or remained unavailable rather than failing.
- [x] Settings, diagnostics, cached browsing, pinning, and other local actions remained usable offline.
- [x] Quitting and relaunching the same QA build while offline restored cached state without a failed-task burst.
- [x] Restoring connectivity cleared the offline state and resumed exactly one full refresh plus one deferred Helm information check without duplicate task bursts.

Owner-assisted validation completed on 2026-08-15. The full focused sequence
passed without an observed regression.

## Automated Gates

- [x] The full repository quality gate passed on 2026-08-15.
- [x] Rust workspace tests, formatting, and Clippy passed.
- [x] Locale integrity, overflow, mirror parity, and channel-policy checks passed.
- [x] The arm64 macOS suite passed all 153 tests while retaining the macOS 13 deployment target.
- [x] Release-state, toolchain, provenance, recovery, and stable/RC appcast-channel contracts passed.
- [x] Non-mutating release preflight and prepare checks passed. The only warning was that GitHub token scopes could not be inferred from response headers.

Version-preparation evidence: on 2026-08-15, the full quality gate passed again
after the Rust workspace moved to `0.19.0-rc.7`, including all 153 arm64 macOS
tests and release contracts. The explicit non-mutating RC7 Sparkle/appcast
checklist and release rehearsal then passed preflight, prepare, and verify.

## Remaining RC7 Gates

- [ ] Review and merge the prepared RC7 version change through the protected branch flow.
- [ ] Repeat the required clean-`main` rehearsal, preflight, canary, and publication-authorization gates before tagging or publishing.
- [ ] Keep published stable and prerelease metadata on `v0.18.2` and `v0.19.0-rc.6` until the RC7 release sequence completes.

## Outcome

- [x] The post-RC6 RC7 candidate scope is accepted on `dev`.
- [x] No release-blocking regression was found in the automated or owner-assisted pass.
- [x] RC7 version preparation may proceed through its own reviewed PR.
- [x] The Rust workspace and local packages identify as `0.19.0-rc.7` while published stable/RC metadata remains on `v0.18.2`/`v0.19.0-rc.6`.
