# Dashboard Add Manager Validation

Tracks [issue #463](https://github.com/jasoncavinder/Helm/issues/463). This is a v0.20 development workflow, not a change to published v0.19.1.

## Contract

- Manager Health always ends with a distinct Add manager button, including when no managers are detected.
- The chooser uses the service's planner-filtered `installMethodOptions`, not `ManagerInfo`'s broader distribution-method registry or a second list of installable manager IDs.
- Only implemented, undetected managers with recognized core method metadata appear. Detected-but-disabled managers remain in Environment and are not installation candidates.
- Policy-blocked methods and missing/disabled/ineligible/busy dependencies remain visible with an explanation. Unknown policy tags and missing eligibility fail closed. A manager can proceed if at least one supported method is ready.
- Service disconnection, offline state, loading, and research previews have explicit presentation. No preview can submit live operations.
- Review Installation closes the chooser, clears any Environment domain filter, scrolls the selected manager into view, reveals its inspector, and opens its existing installation sheet. The one-time scroll waits for the destination rows and cannot consume a newer request. It does not install, enable a manager, or apply preferences.
- `cargo-binstall` supports both Cargo and Homebrew installation. The explicit Cargo method routes through the existing Cargo adapter with `cargo install cargo-binstall --locked`, following the upstream source-install recipe. It requires a ready Cargo dependency, not a complete crates.io catalog or cached search result. Existing default/persisted Homebrew routing is preserved; the unimplemented script method remains excluded.
- The native toolbar places the details toggle after search. Tahoe uses its default search toolbar item; Ventura through Sequoia retain the existing search bridge. Neither window geometry nor sidebar behavior is overridden.
- The selected method is checked again against current metadata and dependencies before submission and after each asynchronous preference callback. A different available method cannot silently replace an unavailable selected method. Missing methods never trigger a direct-install fallback.
- After explicit Install confirmation, the existing core/service task path remains responsible for execution, authorization, progress, cancellation, verification, failure reporting, and recovery. The UI does not introduce shell execution, credential handling, or a second installer.

## Automated Evidence

`ManagerInstallationCatalogTests` covers candidate filtering, disabled undetected sources, absent method metadata, policy and eligibility blocks, every dependency-readiness gate, alternative methods, active work, offline/disconnected/research/loading/empty states, stale selection, exact-method revalidation, and stable ordering. The app-target Xcode build checks production wiring. Locale integrity, length, mirror, and hardcoded-string checks cover the seven shipped locales.

These are presentation-model and build checks, not live installation or automated UI-navigation evidence.

Local validation on 2026-09-23: the arm64 Debug app-target Xcode gate passed 353 tests (13 new), with zero failures or skips, on macOS 26.6.2 using a macOS 13.0 deployment target. This is not a Ventura runtime pass. SwiftLint 0.59.1 strict mode, locale integrity/length/mirror checks, hardcoded UI strings, channel policy, docs sync, and diff whitespace checks passed. The existing XCTest newer-runtime linkage and helper-strip output warnings remain unchanged.

The owner-QA follow-up passed 356 Swift tests and 1,325 Rust workspace tests (zero failures/skips), Rust format/Clippy, strict SwiftLint, i18n, channel policy, and docs sync. Added coverage exercises repeat/late/cancelled manager reveal requests, Cargo dependency gating, planner routing, FFI method metadata, and locked Cargo command arguments. The first rebuilt QA artifact was mistakenly ad-hoc signed: its bundle integrity passed, but the service rejected the app with OSStatus `-67050`. That artifact is excluded from live-data QA evidence. Automated native UI inspection timed out, so scroll positioning and toolbar order still require owner confirmation; no real installation was performed.

After explicit owner approval to use the existing configured signing identity, the same source `82c02d68120e7d8735c556394a81d701010d7886` was rebuilt on 2026-09-23 at 11:12:48 HST with the project's Developer ID app signature. The bundle passed deep/strict integrity verification and the exact Apple-anchor/team/bundle-ID requirement used by `HelmServiceDelegate`. At 11:13:10 HST, logs confirmed `helm_init result: true`, `XPC connection handshake succeeded`, and `helm_trigger_refresh result: true`. Only the corrected QA app and its bundled service remained running. This establishes connection/refresh acceptance, not visual confirmation of manager cards or completed installation.

Corrected artifact SHA-256 values: executable `856869f653482e38c89cc16760c5a4498d51b25f8cae5b7fa95ee7755ecc73c8`, Debug dylib `5ed61cf161457b38fe302a68af572096368b52b9427fb8f2c44bc4b030e7a644`, bundled service `f0888b67473d77004e735eb5ea7860c43c68d52c227c8f1800e13ba873195780`.

## Live QA Preflight

- Obtain explicit approval before using the configured signing identity; never alter/export signing credentials or relax service validation.
- Do not copy the ad-hoc/unsigned Xcode test command into a live-service QA build. Preserve the project's configured app signing settings, without `CODE_SIGN_IDENTITY=-` or `CODE_SIGNING_ALLOWED=NO` overrides.
- Verify bundle integrity **and** the `HelmServiceDelegate` trust requirement before launching. An ad-hoc signature can pass integrity verification while still failing the service requirement.
- Verify the exact executable path and one app instance, then confirm successful service initialization, handshake, and refresh acceptance. Keep the Debug development database namespace and stable installation untouched.
- Only report visible manager data after UI/owner confirmation; passing builds, tests, or signature checks alone do not establish a functioning live-data session.

## Owner QA

Use a signed Debug build in `Helm-Development`, not the stable database. Do not click Install on the owner's Mac merely to validate navigation.

1. Open Dashboard. Add manager is the final, visibly distinct Manager Health card. It is reachable with both Tab and Shift-Tab and announced as a button by VoiceOver.
2. Open the chooser. Installed managers, including disabled installed managers, are absent. Unavailable methods name their policy block or prerequisite. Cancel and Escape close the chooser without creating tasks.
3. Select an available manager and choose Review Installation. Environment opens with that manager selected and scrolled into view, the inspector visible, and the existing installation review sheet open. Confirm the method/source/advanced options, then Cancel. No installation task should appear. Repeat with cargo-binstall: both Cargo and Homebrew methods should be listed; a missing/disabled/busy dependency disables only its corresponding method.
4. Repeat after visiting a filtered Environment route and hiding its inspector. The handoff clears the route filter and reveals the correct inspector. Repeat opening/cancelling to catch presentation-transition regressions.
5. While the chooser or installation review is open, go offline. Review/Install must become unavailable. Reconnect and verify that current readiness is re-evaluated; no task should be submitted automatically.
6. Check Light/Dark and live language changes, especially German, Hungarian, and Japanese. Check selection and both footer buttons with Full Keyboard Access and VoiceOver; long method explanations must wrap without hiding the footer.

Owner QA on 2026-09-23: the four initial review-only checks passed on source `7248a3d528fc9a04c8f78c463e8b6a6516db8e62`, using the signed arm64 Debug artifact at `.worktrees/v020-add-manager-dashboard/artifacts/add-manager-validation/SignedQA/Build/Products/Debug/Helm.app` (Debug dylib built at 01:58:11 HST). This covers the distinct card, chooser, cancellation, selection/inspector handoff, and repeated handoff with a hidden inspector. It does not cover filtered-route handoff, offline, appearance, localization, accessibility, or real installation. Owner observations prompted the scroll, Cargo method, and toolbar refinements above; those changes require a new visual pass, including Command-F and details toggling at minimum window size.

## Isolated Lifecycle Certification

In a disposable macOS test environment, confirm an actual supported manager installation, post-install detection, progress/Activity routing, cancellation, a controlled failure, and recovery. Include no-additions, missing prerequisite, managed-policy-restricted, and unavailable service snapshots. Record the method, source revision, initial/final installed state, and outcome alongside [all-manager certification #500](https://github.com/jasoncavinder/Helm/issues/500). Existing installer reuse does not substitute for this evidence.
