# v0.19 Environment Brief Presentation Validation

Date: 2026-08-08; implementation evidence updated 2026-08-26

## Scope

This checkpoint validates the debug-gated Dashboard-hosted Environment Brief before any production first-run route switch. It covers the versioned `first-useful`, `current`, `partial`, `offline`, and `service-failure` fixtures without accepting legal terms, mutating software, contacting package sources, or using the stable user database.

## Matrix

- All five fixtures rendered at the `860x600` minimum content size and a `1200x800` expanded window size.
- Every size/state combination rendered in deterministic light and dark appearances.
- German, Hungarian, and Japanese rendered in all five states at minimum size in light and dark appearances.
- A temporary copied debug app with every English localization value padded by 40% rendered all five states at minimum size in light and dark appearances.
- The app was built and launched as `arm64` with a dedicated `/tmp` Derived Data directory and an isolated `HELM_DB_PATH` for every launch.

Result: no clipped safety copy, unreachable action, overlap, or contrast-dependent state was found after remediation. The content remains scrollable, expanded layouts preserve the bounded reading width, and localized window titles do not affect fixture operation.

## Findings

### Course Indicator coverage semantics

The partial and service-failure fixtures initially rendered a 100% ring because failed, cancelled, and deferred terminal work counted as mapped coverage. The presentation summary now derives determinate completion from mapped managers only. Regression assertions lock partial coverage to 50%, service failure to 0%, and overlapping observation/failure input to mapped coverage without double counting.

### Text expansion

The 40% expansion pass exposed truncation of the system fact value `Apple silicon`. Environment Brief fact values now support two lines and preserve their intrinsic vertical size. The repeated expansion matrix rendered the full value, and locale-width coverage now budgets actions, statuses, and both trust-strip variants across all supported non-English locales.

## Accessibility Contract Review

- Primary actions are native `Button` controls; `Use Helm Now` and legal acceptance expose the default action keyboard shortcut.
- Decorative symbols are hidden where appropriate, manager and coverage rows combine their visible status text, and the Course Indicator exposes a label plus determinate percentage value.
- State is conveyed by symbol and text in addition to color. Differentiate Without Color adds a dashed ring treatment.
- The Environment Brief defines no custom animation. Native indeterminate progress remains system-owned, so Reduce Motion does not require a parallel custom transition path.
- The header uses one stable semantic subtree and a custom responsive `Layout`, so horizontal and vertical presentation preserve the same accessibility identities rather than replacing the rendered controls.

Owner-assisted VoiceOver and Full Keyboard Access checks were executed after the automated presentation matrix. Basic VoiceOver traversal and keyboard operation passed, but the state title did not appear in the VoiceOver Headings rotor and forward/reverse Full Keyboard Access traversal disagreed within the noninteractive header. On the merged baseline, forward `Tab` stops on the `50%` Course Indicator while reverse `Shift-Tab` skips it.

PR #534 source `2c8726a0` implements the intended correction: the rendered title is a native `GroupBox` label, the Course Indicator remains a separate noninteractive percentage element, and localized Management Readiness rows announce title before count. Responsive-layout and all-locale announcement-order regressions pass, and the full arm64 UI quality gate passes 338 tests with 0 failures.

On 2026-08-25, the owner inspected ordinary wide and Debug-forced vertical layouts in an isolated arm64 Debug artifact whose executable SHA-256 was `3dd0cf112f0c2703974d2d3dded7bf44e08c5b491e5cd6634f2b00e14696a83f` and whose `Helm.debug.dylib` SHA-256 was `bea77b90b62a33db9e3a7d3e68bf362450804dca1f4470af5ac656239a3dd5dd`. The record did not retain an exact source revision, tracked-clean tree, or source-blob equivalence mapping for that pre-commit build. Its positive rotor, focus-geometry, separate-percentage, bidirectional traversal, readiness-order, and layout observations are therefore bounded unattributed evidence, not current-source sign-off, and do not close Issue #388. The same matrix must be rerun against an attributable exact-source artifact.

A later 2026-08-25 attempt retained an app executable SHA-256 of `8964cef7714b6f1ca7be4dfb0012468857d6a7ccb3b1d3da9b1d485dc7e2ca52` and `Helm.debug.dylib` SHA-256 of `72b35019797caa08125a0668e48dbc4e560a37a8801709937976f88bd00b183e`. Independent review found that the changed app and test objects compiled before source commit `4b465342` existed; the commit was created while the build was already running. The stable changed files and bundled resources appear source-equivalent to tree `7e8a425b`, but the artifact was not built from a recorded tracked-clean checkout. Its positive wide/forced-vertical rotor, focus-geometry, Course Indicator order, bidirectional traversal, readiness-order/case, and layout observations are therefore bounded pre-commit evidence and do not close Issue #388. PR #535 code source `410abbf9` additionally formats the combined Course Indicator percentage with Helm's selected locale; the tracked-clean 2026-08-26 owner rerun below supplies the required closure evidence.

On 2026-08-26, the owner repeated the complete matrix against one isolated arm64 Debug artifact built after checkout from tracked-clean merged `dev` source `86f4ef23db5081cfce6c595f7d088e1f323a1023` and tree `0597de3a7817347eb5ce3fd7b6dabc570c88141c`; the worktree remained clean after build and QA. The full arm64 Xcode suite passed 339 tests with 0 failures on macOS 26.6.2 (`25G83`). The executable SHA-256 was `3dd0cf112f0c2703974d2d3dded7bf44e08c5b491e5cd6634f2b00e14696a83f`, and the `Helm.debug.dylib` SHA-256 was `7b210e581e9491475c04d723c4acd9b7411be13be26bfa0c92a2b992c496c6d8`. The exact same artifact passed both ordinary wide and Debug-forced vertical layouts: the state title appeared in the VoiceOver Headings menu with matching rendered focus geometry; the Course Indicator remained a separate element announced as `Software sources, 50%`; forward and reverse Full Keyboard Access traversal were symmetric without an asymmetric noninteractive header or ring stop; and neither layout clipped. This exact-source owner evidence closes Issue #388 and the combined Task 7 accessibility item. It is owner QA rather than participant validation, and it does not approve switching the production first-run route.

Before PR #534, bounded experiments with heading traits, interaction-response suppression, and a combined accessibility representation did not produce correct rotor, traversal, and focus-geometry behavior together. The combined representation detached the focus highlight from the rendered header and over-combined the announcement, so that experimental code was reverted. The attributable exact-source validation above supersedes that open gate.

## Verification

- `FirstRunPresentationSupportTests`: 12 passed.
- `LocalizationOverflowValidationTests`: 7 passed.
- Focused Xcode result: 19 tests, 0 failures.
- Full `arm64` Xcode result: 86 tests, 0 failures.
- UI quality gate, locale integrity/length/mirror parity, documentation sync, and diff checks passed.
- PR #534 responsive-layout and all-locale readiness announcement-order regressions passed.
- The PR #534 full `arm64` UI quality gate passed 338 tests with 0 failures.
- PR #535 code source `410abbf9` passes the full 339-test arm64 UI gate; its all-locale regression verifies the composed title-before-percentage label and locale-specific zero-fraction-digit percent token.
- The tracked-clean merged-`dev` rerun at source `86f4ef23` and tree `0597de3a` passed the full 339-test arm64 Xcode suite and the complete owner-assisted wide/forced-vertical VoiceOver and Full Keyboard Access matrix with the recorded artifact hashes.

## Decision

The five presentation states, appearance matrix, resizing behavior, text-expansion behavior, and targeted Environment Brief accessibility contract are ready. Issue #388 and the combined Task 7 accessibility item are closed by attributable exact-source owner evidence. Keep the existing onboarding route authoritative until the separate default-route change is reviewed and approved; participant and broader cross-cutting validation remain separate gates.
