# v0.19.0 Stable Readiness

This document records the candidate, publication, and verification evidence for
the completed promotion of the published `v0.19.0-rc.7` line to `v0.19.0`
stable. Installed-use evidence remains separate from publication evidence.

## Candidate Baseline

- [x] `v0.19.0-rc.7` was published from `main` revision `a29ecb3924e0f029553ad9209c6bc431852a1ccd` with signed/notarized GUI and direct CLI artifacts.
- [x] RC7 publication metadata converged at `main` revision `feca8ac709d2969115ce5ef1e8026214511ac902`, and runbook verification plus Release Publish Verify and both metadata drift guards passed.
- [x] The owner's stable installation detected, installed, and relaunched RC7 through Sparkle successfully.
- [x] PR `#478` adds the reviewed post-RC7 semantic separation between routine updates and manager/source review findings.
- [x] A local QA build containing PR `#478` passed owner-assisted visual review before merge.
- [x] The stable-preparation branch reconciles the RC7 publication closeout from `main` with merged `dev` before changing version identity.

## Stable Scope

- [x] Consolidate the seven published RCs into one `0.19.0` stable release.
- [x] Include Original Wayfinder Dashboard, Plan, Library, Activity, Environment, Settings, and unified status-item popover foundations.
- [x] Include stable/RC self-update routing, scheduled update checks, actionable notifications, selective Plan execution, truthful third-party Sparkle app handoff, manager compatibility fixes, and system-language default behavior.
- [x] Include offline detection, local-only refresh, network-action deferral, and coalesced reconnection recovery.
- [x] Include the consistent Healthy, Running, Updates Ready, Needs Review, Error, and unavailable/deferred presentation merged in PR `#478`.
- [x] Preserve macOS 13 Ventura as the minimum supported version and retain the audited dependency graph.

## Accepted Deferrals

- [x] Issue `#388` keeps the Environment Brief replacement for first-run onboarding development-gated; the existing production onboarding remains active.
- [x] True direct third-party Sparkle updating remains `0.20.x` work behind a dedicated trusted updater boundary; stable uses Open App to Update.
- [x] Native privileged-helper executor activation remains deferred; stable retains the existing askpass path.
- [x] Selectable themes remain post-1.0 work.
- [x] These deferrals do not regress published RC7 behavior and are not release blockers for the scoped stable promotion.

## Prepared-Source Gates

- [x] The full repository quality gate passed on the prepared `0.19.0` source, including Rust tests, formatting, Clippy, locale integrity/parity, SQLite migration compatibility, release contracts, and all 154 arm64 macOS tests.
- [x] The dry-run Sparkle/appcast checklist and non-mutating `v0.19.0` rehearsal passed preflight, prepare, and stable release-state verification.

## Publication Gates

- [x] Review and merge stable-preparation PR `#479` through protected `dev`, then integrate `dev` through protected `main` in PR `#480` at revision `e3f148fdbaf87c78e2502c6a17ff5e13ae2d4147`.
- [x] From clean exact `origin/main`, repeat rehearsal and preflight and require green release canary `31937189914` plus publication-auth write probe `31937193834`.
- [x] Obtain explicit maintainer confirmation immediately before tag creation and publication.
- [x] Publish `v0.19.0` as a non-prerelease latest GitHub release with signed/notarized GUI and direct CLI artifacts through runs `31937869680` and `31937869677`.
- [x] Merge generated metadata PRs `#482` and `#483`, advancing stable metadata to `v0.19.0` without changing the RC7 prerelease metadata.
- [x] Verify converged `main` revision `1e8c4f55fb7f30995231fa329315b4579622e1a6` with the runbook, Release Publish Verify `31938790171`, Appcast Drift Guard `31938790158`, and CLI Update Metadata Drift Guard `31949587600`.

## Decision

The maintainer elected to skip `v0.19.0-rc.8` after successful RC7 installation
and acceptance of the post-RC7 semantic-status QA build. Every stable gate then
passed, and `v0.19.0` was published on 2026-08-16 with stable GitHub/latest,
appcast, and CLI metadata while the RC7 prerelease channel remained intact.
