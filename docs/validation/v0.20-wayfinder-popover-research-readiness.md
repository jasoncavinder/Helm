# v0.20 Wayfinder Popover Research Readiness

Date: 2026-08-21

Status: owner state, accessibility, localization, placement, and timing matrices complete; whole-workflow dataset seeded; Task 1 ambient-health implementation and owner matrix plus Task 2 Plan, Task 3 production native-table Library/global-search, and Task 4 Activity/recovery projections complete; targeted Sparkle handoff evidence, remaining production projections, Ventura compatibility, and moderated checkpoints open

## Scope

This checkpoint closes the implementation-to-research handoff for Native Mac Experience Slice 20.1. It records the production unified Wayfinder popover, the owner-accepted route correction through PR #490, and a deterministic debug-only state seam in the shipping view.

It does not claim a participant session, an end-to-end dataset-driven workflow, a whole-workflow accessibility pass, a broader multi-display hardware matrix, or v0.20 workflow sign-off. The bounded popover accessibility and currently available two-display placement matrices recorded below are complete.

## Production Fixture Contract

Debug builds accept `HELM_WAYFINDER_POPOVER_FIXTURE` with one of these values:

| Fixture | Condition | Affected route | Key evidence |
|---|---|---|---|
| `healthy` | Healthy | All current | 17 detected sources; no primary action |
| `updates-ready` | Updates Ready | Apps and Packages | Four reviewed-plan updates |
| `running` | Active work | Apps | Determinate 5-of-12 progress |
| `needs-review` | Actionable finding | Tools / `mise` | Multiple-install decision context |
| `error` | Failed work | Apps | Recovery action and error route tone |
| `offline` | Offline/cached | All cached | Saved-state action; refresh unavailable |

The fixture is projected through `WayfinderProjectionProjector` and `WayfinderPopoverPresentationProjector`, then rendered by the production `WayfinderPopoverView`. It does not use the separate design-lab renderer.

While a fixture is active in a Debug build:

- the popover bypasses onboarding so the requested state is immediately reviewable;
- launch, manual, and reconnect-triggered Helm refresh requests are ignored;
- scheduled, information-only, and manual Helm self-update checks are ignored;
- refresh and Helm-update controls retain their production appearance, but requests safely no-op;
- Release builds ignore the environment key.

The fixture replaces popover presentation only. Dashboard destinations continue to read the development profile, and synthetic task IDs do not create matching Plan or Activity records. Route controls can validate destination/domain wiring, but a full moderated session must not use them as evidence for destination content or workflow completion until each destination consumes the whole-workflow dataset through its production presentation path.

## Whole-Workflow Dataset Contract

The canonical `v0.20-whole-workflow-v1` corpus now lives at `docs/validation/fixtures/v0.20-whole-workflow-v1.json`. It supplies stable synthetic records for all seven moderated tasks: partial ambient health and failed verification, a 12-update authority plan, cached/remote package search, partial recovery, dual-install provenance, Settings-to-diagnostics navigation, and the Project WOW brief-to-receipt chain.

`WholeWorkflowResearchDatasetLoader` decodes the corpus, and `WholeWorkflowResearchDatasetValidator` rejects safety drift, incomplete task coverage, unresolved references, and missing task-defining evidence. Debug builds recognize an absolute path in `HELM_WAYFINDER_RESEARCH_DATASET`; Release builds ignore it. Task 1 projects the canonical partial source coverage and applied-but-unverified npm activity through the shipping Wayfinder popover. The hero identifies the failed verification and exact unverified `prettier` state, the context row reports eight of ten sources available with one failed source needing review, freshness remains visible in the header, and System/Tools/Apps/Packages distinguish review/current/cached/error state without relying on one aggregate color. Review Recovery deep-links to exact Activity selection `7001`. Task 2 projects exactly its 12 referenced update records through the shipping Plan rows, inspector, scoped selection/risk behavior, and bounded confirmation. Those records exercise the authoritative, standard, and guarded groups only; the corpus contains no interactive vendor-app handoff. Ten non-OS rows begin selected; the pinned and macOS rows remain visible and excluded. The final synthetic sheet records exact selected rows and execution order, automatic eligibility, and selected privilege/reboot risk; any drift disables Run Plan. The production confirmation also presents interactive eligibility and vendor-app handoffs when the current Plan contains them, but that path requires separate targeted evidence and is not attributed to this corpus. Dashboard and notification shortcuts route into this Plan-owned flow rather than presenting a separate generic preview. Task 3 projects its exact `ripgrep` results through the shipping Library, global search, and package inspector: Homebrew is visible immediately as Cached and Recommended because it already has authority, Cargo arrives through bounded Remote enrichment, and accepting either global result deliberately navigates to that exact Library entity. Setting `HELM_WAYFINDER_RESEARCH_OFFLINE` marks the Cargo result and network-backed proposal Deferred without failed-task fan-out. The Homebrew proposal exposes its source, origin, network requirement, and no-authorization requirement in a read-only confirmation. Task 4 projects one applied-but-unverified npm activity and one App Store source that never started through the shipping Activity destination and contextual inspector. The presentation keeps apply and verification results independent, states the unchanged source explicitly, exposes localized before/after and rollback-limit facts, and preserves the ordered Retry Verification, unavailable Restore, Keep Change, and Copy Diagnostics choices. Retry and Keep stop at a read-only research review, Restore remains disabled because no verified downgrade path exists, and Copy Diagnostics writes only the synthetic strict-redaction summary. While the selector is present, Helm fails closed before service startup, suppresses onboarding presentation without changing its persisted state, and blocks refresh, detection, database-backed snapshots, updater traffic, and package mutation even if the selected corpus cannot be loaded. The remaining workflow slices must project this same versioned corpus into their shipping views before moderated sessions begin.

## Safe Launch

Use a Debug app built from this branch. Debug service builds use Helm's `Helm-Development` database namespace; do not launch this fixture against the stable application bundle or stable user database.

In Xcode, add the fixture key and one state value to the Helm scheme's Run environment. From a terminal, launch the executable directly so it inherits the environment:

```bash
HELM_WAYFINDER_POPOVER_FIXTURE=needs-review /path/to/Helm.app/Contents/MacOS/Helm
```

To inspect Task 1 from the status-item popover, Task 2 through Plan, Task 3 through Library/global search, or Task 4 through Activity in a signed sandboxed build, copy the canonical corpus into Helm's readable container before selecting it. The source remains the versioned authority; the runtime copy is disposable. For Task 1, begin with Helm backgrounded and open the popover; for Task 3, search for `ripgrep`. The app does not start its service or permit package/update work in this mode:

```bash
fixture_source="$PWD/docs/validation/fixtures/v0.20-whole-workflow-v1.json"
fixture_runtime="$HOME/Library/Containers/com.jasoncavinder.Helm/Data/tmp/v0.20-whole-workflow-v1.json"
mkdir -p "$(dirname "$fixture_runtime")"
cp "$fixture_source" "$fixture_runtime"
HELM_WAYFINDER_RESEARCH_DATASET="$fixture_runtime" \
  /path/to/Helm.app/Contents/MacOS/Helm
```

Repeat Task 3's offline variant with the same runtime copy. Cached Homebrew remains visible, while Cargo and the network-backed proposal must appear as Deferred:

```bash
fixture_runtime="$HOME/Library/Containers/com.jasoncavinder.Helm/Data/tmp/v0.20-whole-workflow-v1.json"
HELM_WAYFINDER_RESEARCH_DATASET="$fixture_runtime" \
HELM_WAYFINDER_RESEARCH_OFFLINE=1 \
  /path/to/Helm.app/Contents/MacOS/Helm
```

A signed sandboxed app cannot read an arbitrary worktree path merely because the path is supplied through the environment. If the copy or selected path is unavailable, the research selector fails closed rather than falling back to live data.

Quit other Helm development processes before changing fixture states. Use the same build and dataset version for every compared capture.

Debug builds also accept `HELM_WAYFINDER_POPOVER_BENCHMARK_ITERATIONS` from `1` through `100` when a popover fixture is active. The benchmark opens the production panel, records acknowledgement after `makeKeyAndOrderFront` plus app activation, forces the hosted content's layout/display, records first useful render, emits one machine-readable result line, and terminates. Release builds ignore this key.

```bash
HELM_WAYFINDER_POPOVER_FIXTURE=needs-review \
HELM_WAYFINDER_POPOVER_BENCHMARK_ITERATIONS=31 \
/path/to/Helm.app/Contents/MacOS/Helm
```

For a warm series, discard the first sample and retain the next 30. For a cold process series, launch 30 separate processes with one iteration each. Do not purge system caches; report the run as cold process/no application cache rather than cold operating-system file cache.

## Evidence Already Collected

- The owner approved the fixed 400-by-458 composition and requested only bounded visual refinements.
- Production implementation replaced the legacy mini-Dashboard while retaining shared Course truth, stable deep-link metadata, one context row, and Dashboard-owned depth.
- Owner QA found that all route stages initially opened an unfiltered Environment view. The corrected build then passed owner visual verification for System, Tools, Apps, and Packages routing before PR #490 merged.
- Automated projection and route tests cover priority, route tone, finding context, deep-link domain/entity preservation, offline refresh availability, and fixed footprint.
- Automated Task 1 coverage verifies the canonical partial current/cached/failed/deferred source record, canonical failed-source state, exact failed-verification identity and state, fixed freshness age, domain tones, localized presentation keys and counts, exact Activity `7001` recovery deep link, fail-closed safety/identity/scenario/coverage/source/activity drift, and suppression of live operations.
- On 2026-08-21, the owner completed Task 1's current-host matrix using the canonical `v0.20-whole-workflow-v1` corpus (SHA-256 `da952a0bb69b4bc783391d47d279eb5faffa19abc5996175e4f9c9ecf49f7290`) in an isolated arm64 Debug app built at 13:02 HST on macOS `26.6.1` (`25G76`); the app's `Helm.debug.dylib` SHA-256 was `323409ef8311d5daf18e4115abda2bcd9c4e54409a158dca8303c45dc17662b7`. The fixed popover preserved failed-verification identity, the applied-but-unverified `prettier` explanation, checked freshness, partial coverage, all four distinct route states, and exact Review Recovery routing to Activity item `7001` in light and dark appearance. Full Keyboard Access passed in both directions, and VoiceOver exposed the ordered Helm/freshness header, heading, explanation, route states, and separate recovery action without targeting the decorative ring or merging controls. German, Hungarian, Japanese, and a disposable copy with every English localization value expanded by approximately 40% passed without raw keys, clipping, overlap, or unreachable content. Owner QA exposed and verified corrections for VoiceOver hierarchy/header order and live Service Health localization while Dashboard remained open.
- Automated Task 2 coverage verifies the exact plan ID/state, 12 scenario-bounded records, authority order, 10 selected and two excluded rows, selection-dependent privilege/restart consequences, fail-closed selector behavior, reviewed confirmation scope, and rejection of changed step order, automatic eligibility, or risk.
- Automated Task 3 coverage verifies exact scenario-bounded cached and remote records, delayed remote visibility, offline deferral, the one recommended existing-authority source, proposal network/authorization requirements, bounded confirmation scope, deliberate global-result routing, and fail-closed starting-surface drift. This remains implementation evidence and is reported separately from owner validation.
- Automated Task 4 coverage verifies exact scenario and record/action order, applied-versus-verified truth, the unstarted unchanged source, recovery availability, rollback limits, strict-redaction diagnostics, and fail-closed starting-surface, state, and action-availability drift.
- Task 4 owner evidence is split by exact build provenance:
  - On 2026-08-20, the owner completed the broad static current-host matrix against base revision `42c64760` using the `v0.20-whole-workflow-v1` corpus (SHA-256 `da952a0bb69b4bc783391d47d279eb5faffa19abc5996175e4f9c9ecf49f7290`) in an isolated arm64 Debug app built at 14:09 HST on macOS `26.6.1` (`25G76`); the app's `Helm.debug.dylib` SHA-256 was `091e48ab6da880b5ed9022b590328e89a2449739192ec4eaf66c433deb760bb2`. The applied-but-unverified npm change and never-started App Store source remained distinct at minimum and expanded Dashboard sizes in effective Light and explicit Dark appearance; both inspectors preserved truthful before/after, apply, verification, unchanged-source, and rollback-limit facts. Retry Verification, Keep Change, unavailable Restore with its explanation, and strict-redaction diagnostics remained bounded and read-only. Full Keyboard Access passed forward and reverse traversal without traps or duplicate stops, VoiceOver announced identity, state, facts, availability, reasons, focus return, and diagnostics success coherently, and German, Hungarian, and Japanese passed at minimum width without raw keys, unresolved placeholders, clipping, overlap, or unreachable safety content.
  - That broad pass exposed selected Inspector content retaining the prior locale until reselection. The owner then used a separate arm64 Debug build produced at 15:41 HST from a pre-commit locale-refresh snapshot whose `ControlCenterContextBase.swift`, `InspectorViews.swift`, and `ControlCenterContextTests.swift` source blobs were later committed unchanged as `c1f7daf`; the corrected app's `Helm.debug.dylib` SHA-256 was `9a6f65dffc0ff57d550ad06f91de9b90908571c59e8e30103d04c05769024d7b`. This second build was used only to recheck immediate German-to-System-Default visible-content updates without changing selection and, in a copied instance of that corrected Debug app with every English value expanded by approximately 40%, both activities, the complete inspector, recovery sheets, and action reachability at minimum width. The full current-host matrix was not rerun on `c1f7daf`. This owner evidence does not claim preservation of in-progress inspector state, presented sheets or popovers, scroll position, keyboard focus, or VoiceOver focus across a locale switch. The independent-review automated regression proves structural inspector identity, representative `@State` and `@StateObject` preservation, representative Boolean presentation-state preservation, and no child appear/disappear lifecycle transition; it does not mount the production inspector, a real sheet or popover, a `ScrollView`, a keyboard first responder, or VoiceOver focus. Those production continuity cases remain open unless they are covered by a remediated-head owner or production-integration retest.
- Production and Task 3 fixture rows now share the same Ventura-compatible native Library table. Focused tests protect exact consolidated-member selection and inspector/action identity, while the table supplies native column, selection, keyboard/type-navigation, context-command, horizontal-fallback, and icon-action semantics. The merged-head owner regression now covers those production native-control semantics.
- On 2026-08-18, the owner repeated Task 3's behavior-sensitive checks on reviewed head `aaf3049a` after all eight independent-review findings were remediated. The online pass confirmed progressive Cached-to-Remote disclosure, exact Cargo routing and focus handoff, stable search-session dismissal, minimum-size scrolling, and stable source semantics. The offline pass confirmed Cached Homebrew remained usable, Cargo and the network-backed proposal remained Deferred, the confirmation disclosed network and authorization consequences plus the read-only boundary, source selection stayed stable, and search or refresh produced no new task, reconnect churn, or remote-progress loop. At that checkpoint, this bounded pass made the implementation merge-ready while leaving the expanded light/dark and exhaustive accessibility matrices open for the merged-head pass recorded next.
- On 2026-08-18, the owner completed Task 3's remaining matrix on merged `dev` head `3a22ae32` using the container-readable `v0.20-whole-workflow-v1` corpus in the arm64 Debug app on macOS `26.6.1` (`25G76`). At minimum and expanded Dashboard sizes, both effective Light appearance (Automatic resolving to Light) and explicit Dark appearance kept Homebrew and Cargo distinct, readable, and reachable; Homebrew appeared as Cached and Recommended, Cargo appeared as Remote, and accepting either result opened the exact Library entity with matching inspector semantics. Full Keyboard Access passed forward and reverse traversal through search, both results, accepted-result routing, the Homebrew review action, every confirmation fact, dismissal, and focus return without traps or duplicate stops. VoiceOver announced the toolbar search, each result and source once, origin and recommendation semantics, exact route changes, the review action, manager, result origin, network requirement, no-authorization requirement, read-only boundary, and dismissal without merged or duplicate controls. The host appearance setting was restored to Automatic after the Dark pass.
- On 2026-08-19, the owner repeated the bounded Task 3 matrix against the production native Library table on merged `dev` revision `27e8cc213f001e3f455471bceaefe4751dd194e9`, using the container-readable `v0.20-whole-workflow-v1` corpus in a fresh arm64 Debug app on macOS `26.6.1` (`25G76`). Minimum and expanded Light/Dark layouts preserved four visible columns, distinct Homebrew/Cargo source rows, row content, the trailing action, and horizontal fallback without clipping. Exact row selection, inspector clearing on deselection, accepted global-result routing/focus for each source, native arrow and type selection, context commands, and the read-only Homebrew review all passed. Full Keyboard Access traversed the table and visible action in both directions without trapping focus. VoiceOver identified the table and columns, announced origin/recommendation/version/status facts once, retained an actionable localized icon label, and suppressed decorative duplicates. The fixture remained read-only and did not start service, database, network, updater, or mutation work.
- On 2026-08-18, the owner completed the full Task 2 visual, interaction, and accessibility matrix on the initial implementation head `d2c49922`, then repeated the behavior-sensitive checks on reviewed head `41622479` after all four independent-review remediations. The current-head regression confirmed 12 rows, 10 initially selected rows, all three corpus authority groups, selection and filtering behavior, selected-scope risk, exact read-only confirmation, and Full Keyboard Access and VoiceOver behavior. Minimum and expanded Dashboard sizes plus light and dark appearance had no surprises in the full pass. The host was arm64 macOS `26.6.1` (`25G76`). The sandboxed fixture-path failure found during setup was corrected by using the container-readable runtime copy documented above; it was harness setup, not product fallback or live-data evidence.
- The owner rendered Healthy, Updates Ready, Running, Needs Review, Error, and Offline through the production-view fixture seam in effective Light appearance (Auto resolving to Light) and explicit Dark appearance using the same arm64 Debug build. All twelve state/appearance combinations passed visual checks for hierarchy, legibility, semantic distinction, fixed footprint, clipping, transparency, and layout stability.
- The initial Needs Review/Light pass exposed a literal percent sign before the localized manager name. The locale catalogs used unsupported `%{manager}` placeholder syntax; both locale trees now use `{manager}`, locale parity passes, and regression coverage checks both affected Wayfinder context keys across every supported locale. The corrected Needs Review fixture passed in both appearances.
- The initial Full Keyboard Access pass skipped all four route controls because each native `Button` was replaced by a custom accessibility element. The route controls now retain their native button accessibility identity while preserving explicit labels, semantic values, and hints. Owner re-test passed one complete Tab and Shift-Tab loop containing the route controls and all previously reachable actions, keyboard route activation, Escape dismissal with focus return, and single outside-click dismissal without flicker or reopening.
- The corrected Needs Review fixture passed the owner VoiceOver pass: logical single-pass order, nonduplicated header/course semantics, named primary and utility actions, individual System/Tools/Apps/Packages button roles, route status values and hints, coherent `mise` decision context, and `VO-Space` routing to the Tools-filtered Environment destination with `mise` selected.
- With Increased Contrast, Reduce Transparency, Differentiate Without Color, and Reduce Motion enabled together, owner QA passed Running, Needs Review, Error, and Updates Ready. Progress remained meaningful without animation, surfaces stayed opaque and legible, and state words, icons, ring segmentation/dashes, and actions distinguished activity, review, failure, and routine updates without depending on color. All four system settings were then verified restored to their original off state.
- German, Hungarian, and Japanese Needs Review fixtures passed at the fixed footprint without clipping, overlap, raw keys, or unreachable actions. Hungarian QA exposed three English utility-menu fallbacks, while the catalog audit found one additional German fallback; both locale trees now localize the complete menu, and regression coverage rejects English-equivalent utility labels in every non-English locale. A copied Debug app with every English value expanded by approximately 40% also passed the fixed-footprint content and utility-menu checks.
- The Needs Review fixture passed owner placement checks at the menu-bar screen edge, on each currently available display, and in an external app's full-screen Space. It remained fully onscreen and correctly anchored, did not substitute the Dashboard, and dismissed cleanly through both outside click and Escape without flicker or reopening.
- Opening-time measurements used the production Needs Review panel in a `113 MB` arm64 Debug app on a MacBook Pro (`Mac17,6`, Apple M5 Max, 18 CPU cores, 64 GB memory), macOS `26.6.1` (`25G76`), on AC power. Available displays were the built-in `1728x1117`-point Retina display at `2x` and a `1920x1080` AirPlay display at `1x`. The projection-only fixture represented 17 detected managers and one `mise` finding; it did not read a synthetic database or submit network/update work.
- Thirty warm cached reopenings passed the `50 ms` acknowledgement and `150 ms` first-useful-render p95 budgets: acknowledgement median/p95/worst was `3.897/11.785/15.070 ms`; first useful render was `4.883/12.592/16.611 ms`. Thirty separate cold-process first openings, without purging operating-system file caches, passed the `100 ms` acknowledgement and `300 ms` shell budgets: acknowledgement was `10.145/22.120/22.995 ms`; first useful render was `14.481/25.462/25.869 ms`. p95 uses nearest-rank selection, and the reported worst value preserves the outlier evidence.

This evidence is implementation and owner-QA evidence, not participant evidence.

## Remaining Slice 20.1 Validation

- [x] Render all six production-view fixtures in light and dark appearance.
- [x] Repeat at screen edges and on each available display, including a full-screen Space.
- [x] Verify outside-click and Escape dismissal plus focus return.
- [x] Run Full Keyboard Access in both traversal directions.
- [x] Run VoiceOver order, labels, values, hints, and route announcements.
- [x] Run Increased Contrast, Differentiate Without Color, Reduce Motion, and Reduce Transparency.
- [x] Run German, Hungarian, Japanese, and representative +40% text expansion.
- [x] Record warm/cold opening measurements against the approved quality budget.
- [x] Seed a versioned whole-workflow synthetic dataset before running the seven-task moderated protocol.
- [ ] Project the versioned dataset through every production workflow used by the seven-task protocol. Task 1 popover/Activity routing, Task 2 Plan, Task 3 Library/global-search, and Task 4 Activity/recovery projections are implemented; Tasks 5-7 remain open.
- [ ] Run and report the owner-moderated participant checkpoint.

## Task 1 Validation

- [x] With Helm backgrounded, open the status-item popover at its fixed size in light and dark appearance. Confirm it identifies the failed verification, the applied-but-unverified `prettier` state, checked freshness, and partial coverage before Dashboard opens.
- [x] Confirm the four route stages visibly and accessibly distinguish System needs review, Tools current, Apps cached, and Packages error without relying on color alone.
- [x] Select Review Recovery and confirm Dashboard opens directly to Activity with the failed `prettier` verification selected; the inspector must match Task 4's applied, unverified, and rollback-limit facts.
- [x] Confirm Full Keyboard Access traverses the popover in both directions and VoiceOver announces failure identity, freshness, partial coverage, route states, and Review Recovery without merged or duplicate controls.
- [x] Run German, Hungarian, Japanese, and representative +40% text expansion at the fixed popover size; no identity, freshness, coverage, or recovery content may clip or become unreachable.

## Remaining Task 2 Validation

- [x] Review the 12-row Task 2 Plan and final confirmation at minimum and expanded Dashboard sizes in light and dark appearance, including all three corpus authority groups, excluded pin/macOS rows, and selected risk.
- [ ] Separately review a current Plan containing at least one third-party Sparkle step through the production Plan and final-confirmation path. Confirm the interactive authority group and vendor-app handoff, stop before Run Plan, record the current-plan conditions, and do not attribute this evidence to the 12-row Task 2 corpus.
- [x] Confirm Full Keyboard Access reaches filters, authority disclosure controls, every visible row action, Run Plan, and both confirmation actions in forward and reverse traversal.
- [x] Confirm VoiceOver announces authority groups, package and manager identity, inclusion state, execution sequence, status, risks, and the read-only fixture boundary without duplicate controls.
- [x] Record current-host owner sign-off on arm64 macOS `26.6.1` (`25G76`).
- [ ] Retain the protected macOS 13 `arch=arm64` lane as Ventura compatibility evidence before the first `v0.20.0` release candidate.

## Task 3 Validation

- [x] At minimum and expanded Dashboard sizes in light and dark appearance, search for `ripgrep`; confirm Homebrew appears immediately as Cached and Recommended, Cargo arrives as Remote, source rows remain distinct, and accepting a global result opens the exact selected Library entity.
- [x] Select each source in Library and the inspector, verify the source/origin/recommendation language, and open the Homebrew install review. Confirm the sheet reports network required, authorization not required, and the read-only fixture boundary; do not claim or attempt a real install.
- [x] Repeat with `HELM_WAYFINDER_RESEARCH_OFFLINE=1`; confirm Cached remains usable, Cargo and the proposal are Deferred, no failed task is created, and no remote-progress loop appears.
- [x] Confirm Full Keyboard Access and VoiceOver reach and identify the toolbar search, each result/source, origin and recommendation semantics, the review action, all confirmation facts, and dismissal without duplicate controls.
- [x] Repeat the Task 3 minimum/expanded visual, Full Keyboard Access, and VoiceOver checks against the production native Library table, including column headers, type selection, exact accepted-result focus, context commands, and icon-action labels.

## Task 4 Validation

- [x] At minimum and expanded Dashboard sizes in light and dark appearance, confirm Activity clearly distinguishes the applied-but-unverified npm change from the App Store source that never started.
- [x] Select both activities and verify the inspector presents manager/task identity, Before, After, Apply result, Verification result, and rollback limits without calling the failed verification successful or implying that the unstarted source changed anything.
- [x] Confirm Retry Verification and Keep Change open bounded read-only reviews, Restore is visible but unavailable with its reason, and Copy Diagnostics contains only the synthetic strict-redaction fields.
- [x] Confirm Full Keyboard Access traverses both activity rows, every available recovery choice, the disabled Restore explanation, review dismissal, and inspector visibility in both directions without a trap or duplicate stop.
- [x] Confirm VoiceOver announces package/source identity, applied and verification states, unchanged state, before/after facts, rollback limits, action availability, and the read-only boundary without relying on color.
- [x] Run German, Hungarian, Japanese, and representative +40% text expansion at minimum width; no safety or rollback content may clip or become unreachable.
- [x] Recheck that selected Inspector visible content changes from German to System Default without requiring reselection. This bounded owner check does not include locale-switch preservation of inspector state, presented sheets or popovers, scroll position, keyboard focus, or VoiceOver focus. The independent-review automated regression covers structural identity, representative `@State` and `@StateObject`, representative Boolean presentation state, and child lifecycle continuity only; real sheet or popover presentation, scroll position, keyboard focus, and VoiceOver focus continuity remain open absent a remediated-head owner or production-integration retest.

## Research Records

- Per-session record: `docs/validation/templates/native-macos-research-session.md`
- Aggregate report: `docs/validation/templates/native-macos-research-checkpoint-report.md`
- Governing protocol: `docs/app-design/NATIVE_MACOS_RESEARCH_VALIDATION.md`

## Decision

Slice 20.1 has a production-fidelity, deterministic state seam, a semantically validated whole-workflow corpus, and repeatable research records. Its general owner state, popover accessibility, localization, currently available display placement, opening-time matrices, and dataset-seeding prerequisite are complete. Task 1 now has safe production-popover projection, exact Activity routing, automated contract evidence, and a complete current-host owner visual/accessibility/localization matrix. Task 2 has read-only production-path Plan projection, bounded confirmation parity, and current-host owner sign-off. Task 3 has a safe production-path Library/global-search projection, automated contract evidence, and complete production native-table current-host owner visual, interaction, accessibility, and online/offline validation. Task 4 has a safe production-path Activity/recovery projection, automated contract evidence, a complete static current-host owner visual/interaction/accessibility/localization matrix on base `42c64760`, and a bounded visible-content locale-refresh plus expansion recheck on the source-equivalent pre-commit snapshot later recorded as `c1f7daf`. The independent-review locale-change regression demonstrates structural inspector identity, representative `@State` and `@StateObject`, representative Boolean presentation state, and child lifecycle continuity. Real sheet or popover presentation, scroll position, keyboard focus, and VoiceOver focus continuity remain open absent a remediated-head owner or production-integration retest. Targeted production Sparkle-handoff evidence and the protected Ventura lane remain open alongside Tasks 5-7, whole-workflow accessibility, and moderated research. No milestone exit may cite this readiness record as participant validation.
