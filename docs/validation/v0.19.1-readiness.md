# v0.19.1 Maintenance Readiness

This document records the evidence and remaining gates for the narrowly scoped
`v0.19.1` maintenance release. It does not authorize a tag, workflow dispatch,
GitHub release, artifact publication, or metadata mutation.

## Candidate Baseline

- [x] `v0.19.0` is the current published stable release and owns GitHub `releases/latest`, the default Sparkle appcast item, and stable CLI metadata.
- [x] `v0.19.0-rc.7` remains available as a published prerelease on the isolated beta appcast and RC CLI channels.
- [x] PR `#496` merged the retained Homebrew-keg correction to `main` without dependency, migration, or release-automation changes.
- [x] The release-preparation branch starts from current `origin/main` and identifies the Rust workspace and local packages as `0.19.1`.

## Maintenance Scope

- [x] Normalize retained older Homebrew kegs into one logical manager installation only when the formula identity and Homebrew prefix match.
- [x] Prefer the active or linked current keg as the representative installation while retaining merged paths as aliases.
- [x] Preserve installations from distinct Homebrew prefixes as separate manageable instances.
- [x] Keep inactive installation rows informational: they do not inherit the active executable's version and cannot initiate manager-wide update or uninstall actions.
- [x] Do not delete old kegs, change Homebrew ownership, redesign manager lifecycle behavior, or include `v0.20.x` work.

## Dependency And Persistence Review

- [x] The Rust, Swift Package, website, and GitHub Actions dependency sets are unchanged from `v0.19.0`.
- [x] No SQLite migration is added or modified.
- [x] The 2026-08-11 third-party dependency audit remains applicable; only its release context is updated for the unchanged `v0.19.1` candidate.

## Prepared-Source Gates

- [x] Focused manager-instance tests cover same-prefix collapse, distinct-prefix preservation, linked-current selection, unknown-current preservation, and inactive-version isolation.
- [x] The full repository quality gate passes with `HELM_XCODE_ARCH=arm64`, including Rust tests/formatting/Clippy, locale checks, release contracts, and all 154 macOS tests.
- [x] The standalone SQLite migration compatibility gate passes.
- [x] Documentation sync passes with the published release line still resolving to `v0.19.0` until publication.
- [x] The dry-run Sparkle/appcast checklist, non-mutating `v0.19.1` rehearsal, and release preflight pass on the prepared source.

Prepared-source evidence collected on 2026-08-18:

- `cargo test --manifest-path core/rust/Cargo.toml -p helm-core manager_instances::tests` passed all 13 focused manager-instance tests.
- `cargo test --manifest-path core/rust/Cargo.toml -p helm-core inactive_install_instance_does_not_inherit_active_detection_version` passed the inactive-version regression.
- The built CLI reports `0.19.1`, and generated macOS build metadata reports `MARKETING_VERSION = 0.19.1` with monotonic build number `19001900`.
- `HELM_XCODE_ARCH=arm64 .opencode/skills/run-quality-gate/scripts/run-quality-gate.sh all` passed outside the sandbox after the sandboxed Xcode attempt was correctly discarded for blocked DerivedData/package-cache access.
- `scripts/ci/check_sqlite_migration_compatibility.sh` passed its immutable-manifest, core migration, CLI recovery, and isolated FFI initialization checks.
- `.opencode/skills/docs-sync/scripts/docs-sync-check.sh` and `.opencode/skills/sparkle-appcast-checklist/scripts/run-checklist.sh v0.19.1` passed.
- `scripts/release/rehearsal_dry_run.sh --tag v0.19.1` passed, as did `scripts/release/runbook.sh prepare --tag v0.19.1 --allow-non-main` from the clean committed candidate. Preflight confirmed synchronized `0.19.0` stable appcast/CLI metadata behind the `0.19.1` target, with only the expected unmerged-candidate notice and the existing non-blocking OAuth-scope-header and repository-role bypass-fallback warnings.

## Remaining Gates

- [ ] Review and merge the release-preparation PR into protected `main`.
- [ ] From clean exact `origin/main`, repeat rehearsal and preflight and require a green release canary; with explicit maintainer approval, dispatch and pass Release Publish Auth Check with `write_probe=true`.
- [ ] Obtain explicit maintainer confirmation immediately before tag creation and publication.
- [ ] Publish `v0.19.1` as a non-prerelease latest GitHub release and verify stable metadata advances without changing RC7 prerelease metadata.

## Decision

`v0.19.1` is a corrective maintenance release for a false multi-install signal
and unsafe manager-wide controls on inactive install-instance rows. Its bounded
scope and unchanged dependency/persistence surfaces make a direct stable patch
appropriate after the remaining gates pass.
