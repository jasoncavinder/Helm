# v0.19.0-rc.5 Readiness Validation

This is the focused acceptance pass for changes merged after `v0.19.0-rc.4`.
RC5 publication evidence is recorded below; unchecked installed-candidate items remain
required evidence and are not implied by artifact or metadata publication success.

## Preconditions

- [x] Test a clean development-signed build from current `dev` using the `Helm-Development` database namespace. An environment variable supplied to the app process is not inherited by its embedded XPC service, so app-level `HELM_DB_PATH` isolation is not a valid GUI QA precondition.
- [x] Treat development signing as valid only for ordinary UI and contract checks. The release pipeline has no private signed/notarized candidate mode, so launch-daemon registration evidence must come from the installed RC5 artifact after publication.
- [x] Run a full manager detection before refresh-dependent checks and confirm the expected local managers are present; do not reuse a stale development inventory as acceptance evidence.
- [x] Keep the installed signed RC4 app separate from the development build.
- [x] Confirm macOS notification permission is enabled for Helm when testing delivery and actions.
- [x] Keep the Environment Brief production gate unchanged; Issue `#388` is outside this pass.

## Automated Gates

- [x] Full repository quality gate passes.
- [x] arm64 macOS UI tests pass while retaining the macOS 13 deployment target.
- [x] Locale integrity, locale parity, overflow, and hardcoded-string checks pass.
- [x] Documentation sync passes.
- [x] Candidate rehearsal, preflight, prepare, and verify checks pass without mutation; clean-`main` rehearsal and preflight passed after protected integration.

Automated evidence: on 2026-08-12, the full quality gate passed from the RC5
version-preparation branch based on merged `dev` revision
`f91ee014a8685bb0b625a1f853e4b46525cc011c`, including 139 arm64 macOS tests
and all release contracts. The non-mutating `v0.19.0-rc.5` rehearsal then
passed its preflight, prepare, and verify stages.

Final pre-publication evidence: on 2026-08-13, protected PR `#437` merged the
reviewed privilege-risk correction and all required checks passed. From clean
`main` revision `f423b20d263453ccf258cc519776c7e838d04815`, the Sparkle checklist,
full rehearsal, and preflight passed; Release macOS Canary run `31687166639`
and Release Publish Auth Check write-probe run `31687166770` also passed. The
owner then completed the full ordinary-UI matrix from an Xcode-signed Debug
build using `Helm-Development`; unified process evidence showed the embedded
service running and the isolated database receiving fresh discovery data. The
owner reported every status-item, Sparkle Plan, npm search, notification, and
Settings-routing check below passed. Existing focused policy tests retain
coverage for notification deduplication/visibility and status-item routing.

## Publication Evidence

- [x] Tag `v0.19.0-rc.5` identifies clean `main` revision `7dfdc26f9dbc7d3ae75f105cef7f1d7c147a2da3`.
- [x] GitHub published `v0.19.0-rc.5` as a non-draft prerelease while `releases/latest` remained stable at `v0.18.2`.
- [x] Release macOS DMG run `31690897042` and Release CLI Direct Installer run `31690897034` produced the signed/notarized universal GUI artifact, direct CLI artifacts, checksums, appcast, release notes, and provenance.
- [x] Publication PRs `#441` and `#442` merged RC CLI metadata and the beta appcast/release notes without changing stable pointers.
- [x] Final `main` revision `f5bb91ad643655dd67afae40c80ae2ecc16a2294` passed Release Publish Verify run `31693130289`, Appcast Drift Guard run `31693130136`, CLI Update Metadata Drift Guard run `31693303806`, and `scripts/release/runbook.sh verify --tag v0.19.0-rc.5`.

## Privileged Execution Contract

- [x] Automated Rust coverage confirms an elevated request without a typed privileged operation fails before process spawn.
- [x] Automated adapter coverage confirms MAS, Software Update, Xcode Command Line Tools, Rosetta, and MacPorts mutations emit their expected stable privileged-operation identifiers.
- [x] Automated Rust coverage confirms Mac App Store mutations stay on the existing askpass path even when a trusted privileged executor is configured.
- [x] Automated helper policy coverage accepts only the three fixed Apple `softwareupdate` command shapes and rejects executable substitution, argument injection, Mac App Store operations, and MacPorts operations.
- [x] Release contract coverage confirms the helper binary and launch-daemon plist are embedded at their required bundle paths and that Developer ID packaging signs and verifies the helper independently.
- [x] Until registration and installed-build completion gates pass, confirm RC5 documentation and UI describe the helper as embedded but inactive and do not claim that Helm uses system-managed helper approval at runtime.
- [x] Confirm ordinary non-elevated Homebrew work remains unprivileged and unchanged.

## Native Helper Installed-Candidate QA

These checks run after RC5 publication. They gate later executor activation, not
RC5 publication, because unsigned or development-signed builds cannot establish
the release signing, notarization, installation, or Service Management boundary.

- [ ] Install `v0.19.0-rc.5` through Sparkle from the signed `v0.19.0-rc.4` Developer ID app and confirm the resulting `/Applications/Helm.app` passes strict code-signature assessment.
- [ ] In General Settings, confirm System Authorization begins in the truthful current state and never registers automatically.
- [ ] Choose Register Helper and confirm Helm either reaches Registered or presents Approval Required and opens macOS Login Items without reporting a generic registration failure.
- [ ] If approval is required, approve Helm in Login Items, reactivate Helm, and confirm the state refreshes to Registered.
- [ ] Quit and relaunch Helm and confirm the registered state persists.
- [ ] Choose Unregister Helper and confirm the state returns to Not Registered without leaving an active orphan helper service.
- [ ] Register again only if needed for the later executor-activation slice.
- [ ] Confirm privileged operations still use the existing askpass path; registration alone must not activate native execution.
- [ ] Record denial or unavailable-state behavior encountered during this pass. Exercise update-replacement behavior with the next signed candidate rather than claiming it from the first registering build.

## Status Item Routing

- [x] With Dashboard closed, primary click opens the popover.
- [x] With Dashboard closed, secondary click opens the same popover.
- [x] With Dashboard open, primary click focuses Dashboard without replacing it with the popover.
- [x] With Dashboard open, secondary click opens the popover alongside Dashboard.
- [x] The popover still exposes an unambiguous Quit action.

## Sparkle Plan Actions

- [x] A discovered third-party Sparkle update appears in Plan as an interactive step and is not counted as automatic Upgrade All work.
- [x] The Plan row and inspector both expose the vendor update action.
- [x] The Plan row and inspector both expose Open App as a fallback.
- [x] Automatic Plan work leaves unresolved third-party Sparkle steps visible and provides the existing in-app/background handoff.

## npm Incremental Search

- [x] An empty or one-character trimmed npm query invokes no npm remote search, returns no npm remote results, and surfaces no npm error.
- [x] A two-character npm query follows the normal remote-search path.
- [x] Other manager search behavior remains unchanged.

## Actionable Notifications

- [x] Notifications are enabled by default for a profile with no stored preference and can be disabled in Updates settings.
- [x] A newly observed update snapshot notifies only while both Helm interfaces are hidden.
- [x] Repeating the same update snapshot does not deliver another notification.
- [x] Review Plan opens Dashboard directly to Plan.
- [x] Upgrade All appears only when currently eligible automatic work exists, opens Helm's existing confirmation sheet, and starts no work directly from the notification.
- [x] Disabling notifications removes pending/delivered Helm alerts and prevents subsequent delivery.
- [x] Completing routine tasks produces no “all tasks complete” notification.
- [x] Timeout decisions and remaining interactive Plan-step notifications remain available when enabled.
- [x] Changing Helm's in-app language refreshes notification action labels for later notifications.

## Settings Routing

- [x] Command-Comma opens the native Settings scene from a cold launch.
- [x] Popover and Dashboard Settings routes open that same scene.
- [x] Repeated routes focus one Settings window rather than creating duplicates.
- [x] Xcode logs do not contain “Please use SettingsLink for opening the Settings scene.”

## Outcome

- [x] Every release-blocking finding is fixed and revalidated, or RC5 preparation remains blocked.
- [x] Non-blocking observations are documented separately rather than widening RC5 scope.
- [x] Merge the RC5 version-preparation PR, integrate `dev` into `main`, and repeat mandatory clean-`main` release gates before requesting tag/publication approval.
