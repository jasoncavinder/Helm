# v0.20 Adapter VM Certification

Date: 2026-09-24. Status: **in progress, not RC certification**. Tracks #500
and the uv-specific gate #526. No release or guarded OS mutation is authorized
by this record.

## Environment and Provenance

- Owner-provided disposable Parallels guest, arm64 macOS 27.0, build 26A5378j,
  8 CPUs, 16 GiB RAM. The owner explicitly waived a backup/snapshot.
- Owner installed Apple Command Line Tools after the isolated Python bootstrap
  requested `install_name_tool`. Verified active developer path
  `/Library/Developer/CommandLineTools`, Apple clang 21.0.0, and CLT receipt
  `27.0.0.0.1788430756`.
- Guest work is under `~/helm-certification`; every CLI run uses a new isolated
  HOME/config/cache and explicit `HELM_DB_PATH`. The pre-existing guest Helm
  0.19.1 app and the owner's host installation/database were not used or changed.
- Source baseline: merged `dev` `757d52a3b8d4fa25aead97ca58a631286182a131`.
  Initial CLI binary SHA-256:
  `a06981995e74bbc06dbee854072760db3cdff2230022b8aa8715544cca4ecca1`.
- Corrected CLI used below was built from that source plus this change's narrow
  CLI persistence-completion correction, before commit. SHA-256:
  `6693fdd5611a1499a8e72e319d19cf04ea7727e77e3085089ec70abb302122dc`.
  It is a local Debug CLI, not a signed/release GUI artifact.
- uv 0.12.18 was extracted from the official arm64 archive after checking its
  published SHA-256 (`cf40e0c6a202190ccd9e0406dcfdd5b2d6668a9a5c779b17948963df32aafe5b`).
  Python 3.14.7 was installed as isolated test infrastructure, not by Helm's uv
  adapter. Tests use generated local wheels, not a public package-index lifecycle.
- This OS provides no actual Ventura or Tahoe runtime evidence. A macOS 13 build
  deployment target does not fill that gap.

## Findings

**CLI completion race, corrected in this change.** On the original binary,
`managers detect uv` returned installed=true, but the next `managers show uv`
reported detected=false. Repeated successful refreshes left no cached update,
the final task persisted as running, and a reviewed upgrade was rejected because
no candidate existed. CLI terminal waiting did not drain ordered response
persistence before process exit. Both synchronous paths now await the existing
receipt. Two gated-store regressions failed before the correction and pass after
it. The full Rust quality gate passed, including workspace tests, formatting and
Clippy. No database schema or GUI behavior was changed.

**uv final-tool uninstall, open blocker [#556](https://github.com/jasoncavinder/Helm/issues/556).**
After the successful lifecycle steps below, removing the final tool removed both
its entrypoint and the store directory. Helm reported `uv scope path cannot be
resolved` instead of reconciling the successful removal. The post-command scope
binding assumes that the store survives. Upstream uv's
[uninstall implementation](https://github.com/astral-sh/uv/blob/0.12.18/crates/uv/src/commands/tool/uninstall.rs)
explicitly cleans empty stores and may also remove their empty parent. This needs
a separately reviewed, narrowly scoped postcondition correction. Do not bypass
binding checks globally or reinterpret arbitrary missing stores as empty.

## uv Capability Evidence

| Capability/scenario | Result on corrected CLI | Boundary |
|---|---|---|
| Detect and enable | Pass | Concrete standalone 0.12.18 executable; detection visible to next CLI process |
| Installed inventory | Pass | Two distributions, one with two entrypoints; no duplicate package rows |
| Refresh/outdated | Pass | Candidate 1.1 selected; requirement-excluded 2.0, Python-incompatible 1.2 and unsatisfiable 1.3 excluded; original receipt unchanged |
| Search | Pass | Live adapter installed-tool search plus subsequent local cached search; no remote catalog claim |
| Helm pin/unpin | Pass | Pinned upgrade rejected without mutation; unpin permits reviewed upgrade |
| Upgrade | Pass | 1.0 to 1.1; actual installed state persisted and both entrypoints retained |
| Uninstall preview/removal | Partial | Preview nonmutating, removal preserves sibling; **last-tool removal fails reconciliation (#556)** |
| Known-name install | Pass | Requested 1.1.0 resolves to equivalent observed 1.1 and persists actual spelling |
| Missing-package failure | Pass | Failure reported; committed inventory retained |
| Malformed receipt/recovery | Pass | Refresh rejected, cache retained, restored receipt refresh succeeds |
| Unrelated tool preservation | Pass | Sibling receipt remains byte-identical through other operations |
| Empty/new store, scope conflicts | Pending | Last-tool failure blocks end-of-run empty-store checks; explicit missing-store safety stays authoritative |
| Cancellation/timeouts, authenticated sources | Pending | Not covered by the local-wheel lifecycle |
| GUI/FFI, older macOS, uv 0.12.9 | Pending | Prior host fixtures do not count as this VM matrix |

Reproduce with explicit guest paths (Python 3.11+):

```sh
python3 scripts/tests/uv_vm_cli_lifecycle.py \
  --helm /absolute/path/to/helm \
  --uv /absolute/path/to/uv \
  --fixture /absolute/path/to/isolated_lifecycle.py \
  --artifacts /absolute/path/to/disposable-runs \
  --confirm-disposable-environment
```

The fixture is `core/rust/crates/helm-core/tests/fixtures/uv/isolated_lifecycle.py`.
The harness retains command envelopes, isolated databases and a failure report;
it currently exits nonzero at final-tool removal. It is opt-in and must never be
reported as a passing full lifecycle. Original baseline evidence is retained in
the task worktree's `artifacts/baseline-detection.json` (SHA-256
`4cb8023b412b62bcc6608ce04242eb030c4789eac34ac824c64571c90790d972`) and
`artifacts/uv-commands-initial.json` (SHA-256
`dfe7117536765fa68e5e478def5d381ae2c1076147a9b80c1fa0ce9da1844e4b`). Guest runs:
`baseline-v5pvcb8_` and `uv-real-93ar5tm3` beneath `~/helm-certification/runs`.

## All-Adapter Baseline

Detection-only baseline uses a clean HOME and system PATH, deliberately excluding
the isolated uv bootstrap directory. All 29 registry IDs were queried separately.
An absent-state pass is not an installed-capability certification. The three
policy-blocked adapters returned their documented system-unmanaged reason codes;
no system Python/Ruby mutations were attempted.

| Manager | Baseline observation | Remaining real-environment work |
|---|---|---|
| mise | Correctly absent | Installed scope and declared lifecycle |
| asdf | Correctly absent | Installed scope and declared lifecycle |
| rustup | Correctly absent | Installed scope and declared lifecycle |
| homebrew_formula | Correctly absent | Installed scope, lifecycle, native pins |
| homebrew_cask | Correctly absent | Installed scope and declared lifecycle |
| softwareupdate | Detected macOS 27.0 | Read-only scan; guarded mutation needs an approved scenario |
| macports | Correctly absent | Installed scope and declared lifecycle |
| nix_darwin | Correctly absent | Installed detect/refresh-only scope |
| pnpm | Correctly absent | Installed scope and declared lifecycle |
| npm | Correctly absent | Installed scope and declared lifecycle |
| yarn | Correctly absent | Supported installed-version scope and lifecycle |
| pipx | Correctly absent | Installed scope and declared lifecycle |
| uv | Absent on baseline PATH | Separate partial lifecycle above; #556 blocks certification |
| pip | System executable policy-blocked | Eligible user-owned Python and lifecycle |
| poetry | Correctly absent | Installed scope and declared lifecycle |
| rubygems | System executable policy-blocked | Eligible user-owned Ruby and lifecycle |
| bundler | System executable policy-blocked | Eligible user-owned Ruby and declared scope |
| cargo | Correctly absent | Installed scope and declared lifecycle |
| cargo_binstall | Correctly absent | Installed scope and declared lifecycle |
| mas | Correctly absent | Installed scope plus owner-approved App Store account/app scenario |
| sparkle | Correctly absent; Helm excluded | Vendor app inventory, appcast and permitted interactive update evidence |
| setapp | Correctly absent | Installed detection-only scope; licensed/account-dependent scenario |
| docker_desktop | Correctly absent | Installed status-only scope; VM compatibility constraints |
| podman | Correctly absent | Installed status-only scope; VM compatibility constraints |
| colima | Correctly absent | Installed status-only scope; VM compatibility constraints |
| parallels_desktop | Correctly absent inside guest | Installed detection-only scope; host observation is a separate environment |
| xcode_command_line_tools | Detected receipt/version | Installed/outdated inventory; guarded update needs approved scenario |
| rosetta2 | Correctly absent | Explicitly approved installation scenario |
| firmware_updates | Detected system tool | Read-only history refresh; no firmware mutation capability |

No signed GUI parity, private-index behavior, cross-manager authority/cancellation
or full supported-OS matrix has been certified by this baseline. Keep #500/#526
open. Next: review the CLI correction, resolve #556, rerun uv through final removal,
then install and certify manager families in the disposable guest in bounded batches.
