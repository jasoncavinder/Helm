# v0.14.0-alpha.5 Manager Capability Sweep

Generated: 2026-02-19

## Scope

This sweep validates that all managers defined in the 0.14 registry baseline are exported as implemented by FFI manager status after alpha.1–alpha.5 delivery.

## Checks

- `cargo test -p helm-core --manifest-path core/rust/Cargo.toml`
- `cargo test -p helm-ffi --manifest-path core/rust/Cargo.toml`
- `xcodebuild -project apps/macos-ui/Helm.xcodeproj -scheme Helm -destination 'platform=macOS' -derivedDataPath /tmp/helmtests-deriveddata CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -only-testing:HelmTests test`

## Result

- `ManagerId::ALL` count: 28
- FFI status sweep result: `28/28` marked `isImplemented=true`
- Outcome: PASS

## Included 0.14 manager slices

- `v0.14.0-alpha.1`: metadata + optional/detection-only status export
- `v0.14.0-alpha.2`: container/VM + detection-only adapters
- `v0.14.0-alpha.3`: security/firmware adapters
- `v0.14.0-alpha.4`: optional manager adapters (`asdf`, `macports`, `nix_darwin`)
- `v0.14.0-alpha.5`: Homebrew cask status adapter (`homebrew_cask`)

## Notes

- Optional-manager default-enabled policy remains unchanged: `asdf`, `macports`, `nix_darwin` default disabled without explicit preference.
- Detection-only managers remain detection-only by authority (`sparkle`, `setapp`, `parallels_desktop`) while still implemented for detect/status flows.
- `homebrew_cask` is implemented as read-only status scope in 0.14 (`detect`, `refresh`, `list_installed`, `list_outdated`).
